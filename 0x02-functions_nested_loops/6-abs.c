#include "main.h"

/**
 * _abs - start here
 * @pn: parameter is pn
 *
 * Description: return positive result always
 * Return: positive pn
 */
int _abs(int pn)
{
	if (pn < 0)
	{
		return (-pn);
	}
	return (pn);
}
